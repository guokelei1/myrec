# C62 write-once preference memory: G0 terminal

C62 moved the intervention away from C61's score edges and into a two-phase
Transformer state lifecycle.  History wrote four latent slots without query or
candidate access; query-candidate states then read immutable memory and
interacted listwise.  Proposal and implementation hashes were frozen before
the only authorized outcome.

Three physical GPUs ran the data-free two-interest falsifier.  Every structural
contract passed, all model groups trained, and loss decreased.  Nevertheless,
primary accuracy remained near the binary-interest chance boundary
(`0.484--0.516`), tied the one-slot reduction, and was nearly invariant to
wrong history.  Learned slots had very small between-slot variance.  Thus
ordinary cross-attention write tokens do not supply the claimed multi-
preference binding even on the prerequisite task.

C62 closes before repository data.  C26 internal-A and the 399-request C39
reserve remain outcome-fresh.  Do not run the planned dual-domain fit gate or
rescue C62 with slot count, width, depth, optimizer, epochs, or a selected seed.
The next admissible memory hypothesis must alter the event-to-memory binding
law itself and include Slot Attention / balanced assignment plus the C62 pooled
writer as nearest controls.
