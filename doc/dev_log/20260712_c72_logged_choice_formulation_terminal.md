# 2026-07-12 — C72 logged-choice formulation terminal

C72 inherited the exact C71 formula and tested it on a hash-frozen subset of
already-exposed C47 fit labels after C71's fresh target surface proved
label-empty. It was explicitly descriptive, not independent evidence.

The historical choice gradient improved frozen query cosine nominally, but
did not beat positive-only history or uniform slate centering and lacked stable
true/wrong specificity. This rejects the hypothesis that subtracting the
historical opportunity-set expectation supplies the missing personalized
direction. In this data, the subtraction largely removes or rotates useful
positive-item semantics rather than isolating a robust preference residual.

The next search must leave frozen pooled BGE geometry. Another slate
temperature, residual scale, historical-query subtraction, action weight, or
exposed subset would increasingly fit the experiment history.
