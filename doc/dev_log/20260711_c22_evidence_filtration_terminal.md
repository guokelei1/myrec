# 2026-07-11 — C22 evidence-filtration terminal

C22 tested whether putting evidence reliability into the Transformer residual
algebra—not another output gate—could protect exact recurrence and support
transfer.  The implementation passed exact layerwise Jacobian contracts and all
absolute synthetic behavior gates.  It failed only, but decisively, on matched
controls: repeat was tied at 1.0 for every mode, and supported accuracy did not
consistently exceed dense, parallel or final-projection alternatives.

This closes reliability-ordered block-triangular/prefix-normalized residual
streams as the next PPS primitive.  The result supports the pre-outcome
nearest-neighbour warning: a late recurrence safeguard or ordinary Transformer
already captures the synthetic value, so internal filtration adds complexity
without ranking rent.  Do not promote to real data or revise the synthetic
generator after outcome.

Authority: `reports/pps_c22_synthetic_gate.json`.
