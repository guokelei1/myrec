# 2026-07-12 — C68 population-relative free-energy terminal

C68 tested whether C43's generic history expansion could be removed by
contrasting the user's query--candidate--event log partition with a
population-event partition and subtracting the candidate-free contrast. The
four-way operator exactly canceled lower-order separable functions and passed
all software contracts.

Three A40 seeds nevertheless failed the same scientific checks. Primary
accuracy stayed at `0.535--0.555`, wrong history removed only
`0.051--0.096`, and a fixed zero-event carrier generated correction RMS
`0.340--0.434`. Mean and pooled controls matched or beat the primary in two
seeds. Thus population comparison is not the missing function-identity law:
the reference partition itself supplies a candidate-dependent coordinate
against which a constant factual carrier can act.

No repository data or labels were consumed. C68 closes before any real-data
implementation review. The next search must not add another reference,
centering statistic, density ratio, or population memory to the same equation;
it must construct a correction whose value is algebraically zero for every
request-constant history function while remaining nonzero for a specific
history-to-candidate map.
