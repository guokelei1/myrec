# Baseline configs

No new-direction baseline config is frozen yet. Add one only after E0 admits a
standardized version and record its information boundary, candidate manifest,
budget, and qrels boundary.

