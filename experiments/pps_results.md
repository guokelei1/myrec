# Current PPS result register

No new-direction paper numbers are registered yet. Historical C01–C80 and
R0/round1–5 tables are preserved in the dated legacy archive.

When the new direction produces a paper-table number, copy it verbatim from
the shared evaluator's `metrics.json`, with its run ID, config, dataset
manifest, and frozen endpoint recorded here.
