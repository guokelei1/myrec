# Proposed systems

No proposed-system source tree is currently active. C01–C80 source trees were
historical architecture-search evidence and have been archived.

A future `systems/<hypothesis>/` tree may be created only after a replicated
Failure Card passes
`doc/31_problem_discovery_and_architecture_iteration_protocol.md`. The future
model must keep the LM/Transformer as the end-to-end ranking core; checkpoints
and runs remain outside Git.
