# C66 — Canonical Counterfactual Residual-State Transformer

C66 is the sole mechanical continuation of C65.  It sorts every valid
candidate by a stable SHA-256 item key before the unchanged factual/NULL/wrong
computation, then restores caller order.  No model, loss, data, seed, threshold,
precision, or training setting changes.

C66 is not a new architecture claim.  It may proceed beyond label-free G0 only
if canonical serialization repairs the exact numerical failure.
