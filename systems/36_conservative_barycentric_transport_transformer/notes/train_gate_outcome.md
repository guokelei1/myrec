# C36 conservative barycentric transport terminal outcome

C36 terminated at label-free A0. The authoritative report SHA-256 is
`6446784d20b9b962a75db7be015023e87da83c532c4c5148b3258b12831cc1c3`.

G0 passed all five strict-past authentication checks. Fifteen fixed GPU fits
(five modes by three seeds) completed with paired initialization, equal 16,384
parameters, finite gradients, exact repeat/no-history/no-auth/query fallbacks,
and zero determinism/permutation error. The primary was strongly active versus
D2p (47.5% complete-order and 17.0% top-10 change), global-only (34.83%/5.67%),
uncentered trust (29.83%/2.83%), and relative-only (45.0%/13.17%).

The defining conservation mechanics replicated on untouched C36-A. Across
seeds, 27.37%--27.64% of candidate rows had exact differential abstention,
90.60%--91.17% of active requests mixed admitted and rejected candidates,
global-mean error was at most `2.98e-8`, inactive-state error was exactly zero,
and no candidate write reversed the global direction.

Only one of 36 A0 checks failed, but it is binding. The soft max-norm trust
coefficient changed 22.17% of complete orders relative to the unbounded
barycentric reduction, yet only 2 of 600 top-10 sets (0.333%) changed, below the
frozen 0.5% minimum. Thus the extra trust shrinkage is not load-bearing at the
ranking surface that matters. It cannot be rescued by lowering the activity
threshold or changing the coefficient after observing C36-A.

No A label opened. Delayed-B, escrow, dev, and test remain untouched. A
successor may separately freeze the simpler barycentric-conservation primitive
without the failed soft trust shrinkage, but C36 itself is closed.
