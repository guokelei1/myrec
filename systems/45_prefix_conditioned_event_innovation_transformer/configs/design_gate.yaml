candidate_id: c45
gate_id: c45_prefix_conditioned_event_innovation_v1
run_id: 20260712_synthetic_c45_event_innovation_v1
status: prelock_draft

paths:
  candidate_root: systems/45_prefix_conditioned_event_innovation_transformer
  proposal_lock: systems/45_prefix_conditioned_event_innovation_transformer/notes/proposal_lock.json
  artifact_root: artifacts/c45_prefix_conditioned_event_innovation_transformer/design_gate_v1
  checkpoint_root: models/c45_prefix_conditioned_event_innovation_transformer/design_gate_v1
  promoted_report: reports/pps_c45_design_gate.json

resources:
  seed_to_physical_gpu:
    "20262901": 0
    "20262902": 1
    "20262903": 2

generator:
  seed: 20262900
  input_dim: 12
  history_length: 6
  candidate_count: 12
  train_requests: 4096
  validation_requests: 1200
  query_strength: 1.0
  history_strength: 1.35
  event_noise: 0.16
  candidate_noise: 0.22
  hard_negative_fraction: 0.55

model:
  width: 48
  heads: 4
  ff_multiplier: 2
  max_history: 6
  modes:
    - innovation
    - ordinary_delta
    - factual_state
    - raw_event

training:
  seeds: [20262901, 20262902, 20262903]
  steps: 360
  batch_size: 64
  learning_rate: 0.002
  weight_decay: 0.0001
  gradient_clip_norm: 1.0
  base_loss_weight: 0.25

evaluation:
  deterministic_tolerance: 0.0
  candidate_permutation_tolerance: 0.000001

gate:
  primary_gain_over_base_min_each_seed: 0.035
  primary_margin_over_each_control_mean_min: 0.010
  primary_margin_over_each_control_positive_seeds_min: 2
  primary_wrong_gain_retention_max: 0.45
  primary_shuffle_gain_retention_max: 0.45
  primary_clicked_direction_min_each_seed: 0.0
  primary_order_change_fraction_min_each_seed: 0.10

authorization:
  repository_data: false
  repository_labels: false
  dev: false
  test: false
  qrels: false
  synthetic_gpu_after_lock: true
  real_train_gate: false
  full_training: false
