# C34 train-only protocol

Hash-select a wholly fresh 10,000-request strict-nonrepeat fit cohort, 600 A,
600 delayed-B, 600 escrow, and fresh structural controls before opening any
C34 label.  Exclude every C32/C33 target role.  Prior wrong-history donors may
enter because their own target query/candidates were never materialized,
scored, or labeled; only their histories appeared as label-free corruptions.
Train the three
registered modes for seeds 20260961/62/63 with identical rank 16, one epoch,
full candidate lists, request order, optimizer, and equal listwise/direction
losses.

Both controls train before A labels.  A1 compares the cone primary with D2p,
forced-softmax target attention, and global tangent transport under one fixed
fold partition.  No hyperparameter sweep, second attempt, delayed-B rescue,
dev, or test is authorized.
