# C60 nearest-neighbor audit

Status: pre-outcome; global novelty unestablished.

| Neighbor | Shared mechanism | Binding distinction/control |
|---|---|---|
| C28 margin-local contest | Strong-base margins localize pairwise evidence | C28 uses all-pair kernel-weighted Borda and a learned odd comparator; C60 uses only base-order edges, fixed C59 evidence, one-sided capacity, and conservative divergence. C28 remains a negative historical nearest neighbor. |
| Differentiable Sorting Networks ([Petersen et al., 2021](https://proceedings.mlr.press/v139/petersen21a.html)) | Local compare-exchange operations | C60 is not claimed as a sorting invention; its falsifier is the history-conditioned one-sided residual capacity. `hard_adjacent` exposes the direct comparator reduction. |
| HodgeRank ([Jiang et al., 2008](https://arxiv.org/abs/0811.1067)) | Pairwise edge flows aggregated into global scores | Graph divergence is prior art, not the claim. Conservation and adjacency are contracts; utility must beat the registered pairwise controls. |
| C59 | Frozen semantic history direction | `direct_additive`, `history_axis_adjacent`, `raw_query_adjacent`, and `wrong_history` isolate the residual interface from semantic scoring and history specificity. |

A positive exposed-role result would still require a fresh protocol and a
trainable Transformer nearest-neighbor audit.  C60 cannot claim novelty from
pairwise ranking, graph flow, Bradley-Terry probabilities, or sorting alone.
