# C56 mechanism outcome

Status: **terminal failure at label-free A0**.

The clean v3 execution completed three seeds and four equal-capacity modes on
the C26 fit-only 4,800/1,200 split.  The two earlier pre-outcome aborts were
boolean aggregation defects only; no contextual/model/ranking outcome or
holdout label preceded v3.  All candidate hashes, paired initialization,
capacity, finite training, determinism, candidate permutation, real
no-history base, and exact-recurrence item-only contracts passed.  Seed
20263703 failed the frozen all-mode loss-trend check.

The primary failed every load-bearing condition.  Its three seeds changed
`3/0/0` of 1,200 complete base orders and no Top-10 set.  Wrong history changed
`1/0/0` complete orders and no Top-10 set; removing the leave-one-out candidate
message changed `10/0/0` complete orders and no Top-10 set.  The ensemble
changed only `2/1,200` base orders, `1/1,200` under wrong history, and
`4/1,200` under edge ablation, with zero Top-10 changes throughout.  Holdout
fit labels therefore remained closed; C26 internal-A/delayed-B/escrow,
dev/test, and qrels were never opened.

The fixed post-terminal label-free diagnostic localizes the collapse.  The
history-free raw-candidate control was strongly active, changing
795--1,030/1,200 complete orders and 101--524 Top-10 sets, so capacity and the
candidate-list readout were trainable.  By contrast, the primary correction
standard deviation was only `1.8e-11`--`3.2e-6`; two seeds formed an exact-zero
carrier, while the remaining seed formed a very large but candidate-common
carrier that candidate centering removed.  Unprojected and pooled controls
were intermittently active, confirming that the explicit
`x - query_explained(x)` plus factual/null token-state difference introduced
an easy reject-write/common-mode solution.

Decision: close query-complement subtraction, factual/null token-write
differencing, and post-carrier candidate competition as this combined law.
Do not rescue it with residual scale, nonzero head initialization, width,
epoch, loss, seed, threshold, subset, or output normalization changes.  The
next admissible intervention must make candidate competition occur *inside the
history-attention normalization itself*, before a candidate-common carrier can
form, and bind ordinary candidate-to-history attention plus raw-list controls.

Authoritative report SHA-256:
`6af4a7249d76d37f929cd81f3132a78d122d1184cd4ffa6c0b0c67db1d5f356f`.
