# C54 mechanism outcome

Status: `failed_mechanics_terminal`.  C54 is closed on the exposed,
label-unopened C53 mechanics surface.  No A, fresh reserve, dev, test, or qrels
label was opened.

The data-free D0 passed every contract: zero history produced exact base, zero
carrier, and zero list message; candidate permutation and determinism held;
raw-candidate values were history-invariant; changing a distractor affected the
primary target but not the independent target; and every load-bearing gradient
group was active.

All six fit-only GPU runs also converged by the frozen epoch-mean criterion.
On Kuai, carrier list edges changed 21.3%--34.8% of full orders, but only
3/5/8 of 600 Top-10 sets.  Removing factual-minus-null contrast changed only
0/1/0 Top-10 sets, and wrong history changed only 0/2/0.  The primary therefore
failed the binding Top-10 load-bearing checks in every seed.  True/wrong
correction correlation improved from C53's 0.998--0.999 to 0.963--0.976, but
was still too high.

Amazon passed the structural thresholds, but this is not a positive result.
Its correction standard deviation was about 1.29--1.50 against base standard
deviation 0.087, so every Top-10 was overwritten; true/wrong corrections still
correlated 0.992--0.994.  On Kuai, correction/base correlation was 0.854--0.871
and the ensemble changed only 7/600 Top-10 sets against strong D2p.  Thus C54
removed the direct raw-list value shortcut but learned mostly generic/base-
aligned semantic relevance rather than a user-specific complement.

No width, scale, layer, epoch, threshold, seed, or domain rescue is allowed.
The next design must use common score units across domains and change the
representation/training target that identifies history-specific residual
direction; another attention container or post-hoc amplitude change is not
justified.
